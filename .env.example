# Environment Variables
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
JWT_SECRET=your_jwt_secret_key_here
PORT=3000

# Instructions:
# 1. Sign up at https://stripe.com
# 2. Go to Developers > API Keys
# 3. Copy your test keys and replace the values above
# 4. Set up a webhook endpoint for subscription events
# 5. Generate a strong JWT secret (you can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")

# Subscription Configuration:
# - Monthly subscription at $49.99/month
# - Automatically renews every month until cancelled
# - Users can cancel anytime and maintain access until end of billing period
# - All sales are final and non-refundable
# - No long-term commitment required
